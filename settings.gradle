pluginManagement {
    resolutionStrategy {
        repositories {
            mavenCentral()
            mavenLocal()
        }
    }
    plugins {
        id "${quarkusPluginId}" version "${quarkusPluginVersion}"
    }
}
rootProject.name = 'srmj-trace-reproducer'
